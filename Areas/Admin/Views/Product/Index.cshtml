@model IEnumerable<web_quanao.Areas.Admin.Controllers.ProductController.ProductVm>
@{
    ViewBag.Title = "S?n ph?m";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<h1 class="h3 mb-3">Danh sách s?n ph?m</h1>
@if(TempData["Msg"]!=null){<div class="alert alert-success">@TempData["Msg"]</div>}
<p><a class="btn btn-sm btn-primary" href="@Url.Action("Create")">Thêm s?n ph?m</a></p>
<table class="table table-striped table-bordered align-middle">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Tên</th>
            <th>Giá (VN?)</th>
            <th>T?n kho</th>
            <th style="width:160px;">Hành ??ng</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var p in Model)
    {
        <tr>
            <td>@p.Id</td>
            <td>@p.Name</td>
            <td>@string.Format("{0:N0}", p.Price)</td>
            <td>@p.Stock</td>
            <td>
                <a class="btn btn-sm btn-warning" href="@Url.Action("Edit", new { id = p.Id })">S?a</a>
                <a class="btn btn-sm btn-danger" href="@Url.Action("Delete", new { id = p.Id })" onclick="return confirm('Xóa s?n ph?m?');">Xóa</a>
            </td>
        </tr>
    }
    </tbody>
</table>